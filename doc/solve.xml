<!-- %W  minimal.xml         Images documentation        Christopher Jefferson -->
<!-- %H                                                                        -->
<!-- %Y  Copyright (C) 2016, School of Comp. Sci., St Andrews, Scotland        -->


<!-- %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%% -->
<Chapter Label="MinimalChapter">
<Heading>Minimal and Canonical Images</Heading>

Given a group <M>G</M> and action <M>A</M>, the minimal image of an object <M>O</M> is the smallest image
of <M>O</M> under any element of <M>G</M>, under the action <M>A</M>.
<P/>
As a more concrete example, let us consider the minimal image of the set <C>[2,3,5,7]</C> under a group <M>G</M>.
<P/>
We can calculate all the images of our set under <M>G</M>, then choose the smallest one.

<Example><![CDATA[
gap> G := Group((1,2,3)(4,5,6)(7,8,9),(1,4,7)(2,5,8)(3,6,9));;
gap> List(G, g -> OnSets([2,3,5,7], g) );
[ [ 2, 3, 5, 7 ], [ 1, 2, 4, 9 ], [ 1, 3, 6, 8 ], [ 2, 4, 8, 9 ], 
  [ 1, 6, 7, 8 ], [ 3, 5, 7, 9 ], [ 1, 5, 6, 8 ], [ 3, 4, 5, 7 ], 
  [ 2, 4, 6, 9 ] ]
gap> Minimum(List(G, g -> OnSets([2,3,5,7], g) ) );
[ 1, 2, 4, 9 ]
]]></Example>

This is very inefficient, as it requires enumerating all members of <M>G</M>.
The images package produces a function <Ref Func="MinimalImage"/>, which performs this same operation more efficiently.

<Example><![CDATA[
gap> LoadPackage("images", false);
true
gap> MinimalImage(G, [2,3,5,7], OnSets);
[ 1, 2, 4, 9 ]
]]></Example>

The most common use of <C>MinimalImage</C> is to categorise objects into equivalence classes. This next example shows <C>[2,3,5,7]</C> and <C>[1,6,7,8]</C> are in the same orbit, while <C>[3,5,7,8]</C> is in a different orbit.

<Example><![CDATA[
gap> MinimalImage(G, [2,3,5,7], OnSets);
[ 1, 2, 4, 9 ]
gap> MinimalImage(G, [1,6,7,8], OnSets);
[ 1, 2, 4, 9 ]
gap> MinimalImage(G, [3,5,7,8], OnSets);
[ 1, 2, 6, 8 ]
]]></Example>

The <C>result</C> configuration option changes what is returned: the image
itself (<C>GetImage</C>, the default), a permutation performing the mapping
(<C>GetPerm</C>), or just whether the object already is its own image
(<C>GetBool</C>, which is often much faster to compute).

<Example><![CDATA[
gap> p := MinimalImage(G, [2,3,5,7], OnSets, rec(result := GetPerm));
(1,3,2)(4,6,5)(7,9,8)
gap> OnSets([2,3,5,7], p);
[ 1, 2, 4, 9 ]
gap> MinimalImage(G, [2,3,5,7], OnSets, rec(result := GetBool));
false
]]></Example>

In this situation, we do not really need the minimal image, just a method of telling if two sets are in the same equivalence class.
<P/>
Motivated by this, this package provides <Ref Func="CanonicalImage"/>.
<C>CanonicalImage(G,O,A)</C> returns some image of <M>O</M> by an element of <M>G</M> under the action <M>A</M>, guaranteeing that if two objects O1 and O2 are in the same orbit of <M>G</M> then <C>CanonicalImage(G,O1,A) = CanonicalImage(G,O2,A)</C>. However, the canonical image is not "minimal" under any sensible ordering. The advantage of <C>CanonicalImage</C> is that it is much faster than <C>MinimalImage</C>, often by orders of magnitude.
<P/>
<B>WARNING:</B> The value of <C>MinimalImage</C> will remain identical between versions of &GAP; and the &Images; package, unless bugs are discovered. This is <E>not</E> true for <C>CanonicalImage</C>.

<Section Label="ImagesSupportedActions">
<Heading>Supported objects and actions</Heading>

<Ref Func="MinimalImage"/>, <Ref Func="CanonicalImage"/> and their variants
accept the following combinations of object and action. The action argument
defaults to <C>OnPoints</C> in every case, which for lists is not a
supported action, so for lists the action should always be given.

<List>
<Item>a set of positive integers, with <C>OnSets</C>;</Item>
<Item>a list of positive integers, with <C>OnTuples</C>;</Item>
<Item>a set of sets of positive integers, with <C>OnSetsSets</C>;</Item>
<Item>a list of sets of positive integers, with <C>OnTuplesSets</C>;</Item>
<Item>a positive integer, with <C>OnPoints</C>;</Item>
<Item>a permutation, transformation or partial permutation, with
<C>OnPoints</C> (that is, up to conjugacy);</Item>
<Item>a digraph (from the <Package>Digraphs</Package> package), with
<C>OnDigraphs</C> (the group must move only vertices of the digraph,
and multidigraphs are not supported);</Item>
<Item>a fundamental structure (see Chapter
<Ref Chap="FundamentalAndCombinatorialStructures"/>), with
<C>OnFundamental</C>.</Item>
</List>

For digraphs the order minimised is the sorted list of arcs, compared
lexicographically, so the minimal image is the relabelling whose arc
list <C>Set(DigraphEdges(D))</C> is smallest. This is neither the
ordering of <C>&lt;</C> on digraph objects nor the lexicographic order
on adjacency matrices read row by row as 0/1 strings. An undirected
graph is represented by a symmetric digraph and needs no separate
treatment.

<P/>

Not every configuration option applies to every combination: the
<C>stabilizer</C> and <C>getStab</C> options only affect sets,
transformations, permutations, partial permutations and digraphs, and
the <C>order</C> option is ignored for <C>OnSetsSets</C>. See
<Ref Var="ImagesAdvancedConfig"/> for the details.

</Section>

<Section Label="Function Documentation solve">
<Heading>Function documentation</Heading>

<#Include Label="MinimalImage" />

<#Include Label="IsMinimalImageLessThan"/>

<#Include Label="MinimalImagePair" />

<#Include Label="CanonicalImage" />

<#Include Label="ImagesAdvancedConfig" />

</Section>

</Chapter>
